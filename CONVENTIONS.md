# Conventions

_27 rules configured. Auto-generated by `enforcer sync-doc`. Do not edit by hand._

## ERROR — must fix before commit

### branch-naming

Branch '<matched_value>' doesn't match required pattern: type/description

**Why:** Branches encode intent; CI/greps depend on the type/ prefix to route checks and changelogs.

**Applies to:** * (metadata rule, checked once per run)

**Fix:** Rename: git branch -m <type>/<description>

---

### class-capwords

Class '<matched_value>' at <file>:<line> must be CapWords (PascalCase)

**Why:** CapWords (PascalCase) is the Python convention for classes (PEP 8). Distinguishes types from functions at a glance.

**Applies to:** enforcer/**/*.py

**Fix:** Rename to CapWords.

_Checked on changed lines only (`--staged`)._

---

### combinator-test-paired

No test file for combinator <file>. Every combinator needs paired tests.

**Why:** Combinators compose matcher logic; untested combinators can invert or short-circuit the intended logic.

**Applies to:** enforcer/combinators/*.py

**Excludes:** enforcer/combinators/__init__.py

**Fix:** Create tests/test_combinators/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### commit-message

Commit message '<matched_value>' doesn't follow Conventional Commits

**Why:** Conventional Commits enable automated changelog generation and semantic versioning. Unstructured messages break tooling.

**Applies to:** * (metadata rule, checked once per run)

**Fix:** Use: type(scope): description (e.g. feat(matchers): add X)

---

### conventions-md-stale

CONVENTIONS.md is stale or missing. Regenerate after changing rules.

**Why:** A stale conventions doc misleads agents — they follow rules that no longer match the actual config. The doc must be regenerated whenever RULES changes, and direct edits to CONVENTIONS.md must not drift it from the config.

**Applies to:** * (metadata rule, checked once per run)

**Fix:** Run: enforcer sync-doc

---

### core-test-paired

No test file for core module <file>.

**Why:** Core modules (rule, runner, context, config) are load-bearing; untested core changes can break every rule.

**Applies to:** enforcer/*.py

**Excludes:** enforcer/__init__.py, enforcer/matchers/**, enforcer/predicates/**, enforcer/combinators/**, enforcer/parsers/**, enforcer/extractors/**

**Fix:** Create tests/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### cyclomatic-complexity

Function at <file>:<line> has cyclomatic complexity <matched_value> (max 10). Reduce branching.

**Why:** High cyclomatic complexity means too many branches — hard to reason about, test, and maintain. Extract branches into helpers or use early returns.

**Applies to:** enforcer/**/*.py

**Excludes:** enforcer/cli.py

**Fix:** Extract branches into helper functions or use early returns.

_Checked on changed lines only (`--staged`)._

---

### docstring-public

Function '<matched_value>' at <file>:<line> missing docstring. Public functions must be documented.

**Why:** Public functions are the API surface. Without docstrings, users (and agents) must read the implementation to understand intent — that's a failure of the contract.

**Applies to:** enforcer/**/*.py

**Fix:** Add a docstring: """<one-line description>."""

_Checked on changed lines only (`--staged`)._

---

### extractor-test-paired

Extractor <file> has no paired test. Create tests/test_extractors/test_<stem>*.py

**Why:** Extractors are pure string transforms — trivial to test. Missing tests mean regressions in key extraction go unnoticed.

**Applies to:** enforcer/extractors/*.py

**Excludes:** enforcer/extractors/__init__.py, enforcer/extractors/core.py

**Fix:** Add a test file covering happy path, empty/malformed input, and format-specific edge cases.

_Checked on changed lines only (`--staged`)._

---

### function-max-lines

Function at <file>:<line> has <matched_value> lines (max 75). Split or extract.

**Why:** Long functions do too much and are hard to test, read, and review. Splitting forces single-responsibility and improves testability.

**Applies to:** enforcer/**/*.py

**Excludes:** enforcer/cli.py

**Fix:** Extract sub-functions or move logic to a helper module.

_Checked on changed lines only (`--staged`)._

---

### function-max-params

Function at <file>:<line> has <matched_value> parameters (max 5). Group into a dataclass.

**Why:** More than 5 params signals the function does too much; group into a dataclass to make the boundary explicit and the call site readable.

**Applies to:** enforcer/**/*.py

**Excludes:** enforcer/cli.py

**Fix:** Group related parameters into a dataclass and pass as single arg.

_Checked on changed lines only (`--staged`)._

---

### function-snake-case

Function '<matched_value>' at <file>:<line> must be snake_case

**Why:** snake_case is the Python convention (PEP 8). Deviating creates inconsistency that makes code harder to scan.

**Applies to:** enforcer/**/*.py

**Fix:** Rename to snake_case.

_Checked on changed lines only (`--staged`)._

---

### matcher-test-paired

No test file for matcher <file>. Every matcher needs paired tests.

**Why:** Untested matchers ship false positives/negatives silently. Paired tests catch regressions before they reach users.

**Applies to:** enforcer/matchers/*.py

**Excludes:** enforcer/matchers/__init__.py

**Fix:** Create tests/test_matchers/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### no-bare-except

Bare except: at <file>:<line>. Use except Exception or more specific.

**Why:** Bare except catches SystemExit and KeyboardInterrupt, masking intentional exits and making debugging impossible.

**Applies to:** enforcer/**/*.py

**Fix:** Change to `except Exception:` or a more specific exception.

---

### no-print

print() found in library code at <file>:<line>. Use sys.stderr.write or structlog.

**Why:** print() writes to stdout, which is reserved for machine-readable output in CLI tools. Mixing human prose into stdout breaks piping and scripting. sys.stderr is the correct channel for human-facing diagnostics.

**Applies to:** enforcer/**/*.py

**Fix:** Replace print() with sys.stderr.write(...).

---

### no-secrets

Possible hardcoded secret at <file>:<line>. Use env var.

**Why:** Hardcoded secrets ship to the repo and can't be rotated without a commit. Env vars separate config from code and keep secrets out of version control.

**Applies to:** **/*.py

**Excludes:** **/test*, **/*test*

**Fix:** Move to env var or secrets manager.

---

### no-wildcard-imports

Wildcard import at <file>:<line>. Use explicit imports.

**Why:** Wildcard imports pollute the namespace and hide dependencies. Explicit imports make it clear where symbols come from and avoid name collisions.

**Applies to:** enforcer/**/*.py

**Fix:** Replace `from X import *` with explicit symbol imports.

_Checked on changed lines only (`--staged`)._

---

### predicate-test-paired

No test file for predicate <file>. Every predicate needs paired tests.

**Why:** Predicates filter matches; untested predicates can silently suppress real violations or let false ones through.

**Applies to:** enforcer/predicates/*.py

**Excludes:** enforcer/predicates/__init__.py

**Fix:** Create tests/test_predicates/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### readme-max-lines

README.md has <matched_value> lines (max 300). LLM analyzed what doesn't belong.

**Why:** A README over 300 lines is too long for a landing doc. Bloat hides the getting-started path; details belong in docs/.

**Applies to:** README.md

**Fix:** Remove or trim the sections flagged by the LLM response below.

**LLM check:** You are reviewing a README.md that exceeds 300 lines. Identify the specific sections that don't belong in a README and make it too long. For each section, explain why it should be removed or trimmed. Be concrete — reference section headings and line ranges. Common bloat: full install logs, API reference dumps, changelogs, verbose examples, duplicated content.
**Model:** None

---

### todo-needs-owner

TODO/FIXME without owner at <file>:<line>. Use '# TODO(@name): …' or remove.

**Why:** TODOs without owners never get done. An owner reference makes responsibility explicit and enables grepping for open work.

**Applies to:** enforcer/**/*.py

**Fix:** Add owner reference or delete the TODO and address now.

_Checked on changed lines only (`--staged`)._

---

## WARN — blocks unless acknowledged

### commit-msg-aligns-with-changes

Commit message may not align with changes. LLM: <matched_value>

**Why:** A commit message that doesn't describe the actual changes misleads future archaeologists using git log/blame. The LLM sanity check catches gross mismatches.

**Applies to:** * (metadata rule, checked once per run)

**Fix:** Rewrite commit message to describe the actual changes.

---

### verify-config-changed

Config loader changed in <file>. load_config executes enforcer_config.py as module. Run: pytest tests/test_config.py

**Why:** config.py executes enforcer_config.py as a module. Changes here affect how every rule is loaded.

**Applies to:** enforcer/config.py

**Fix:** Verify: RULES/WORKSPACE/SEVERITY_ACTIONS/LLM_CONFIG extracted correctly, defaults work.

_Checked on changed lines only (`--staged`)._

---

### verify-context-changed

FileContextBuilder changed in <file>. Parse-once cache drives all AST matchers. Run: pytest tests/test_context.py tests/test_parse_once.py

**Why:** context.py owns the parse-once cache. A broken cache means every AST matcher re-parses or gets stale ASTs.

**Applies to:** enforcer/context.py

**Fix:** Verify: AST populated lazily, cache hits don't reparse, needs_for_file aggregates correctly.

_Checked on changed lines only (`--staged`)._

---

### verify-parser-changed

Parser changed in <file>. Tree-sitter parse affects all AST matchers. Run: pytest tests/test_parsers.py tests/test_parse_once.py

**Why:** The tree-sitter parser feeds all AST matchers. Changes here can silently break AST detection for Python, TS, or CSS.

**Applies to:** enforcer/parsers/*.py

**Fix:** Verify: Python/TS/CSS ASTs parse correctly, language_for_path maps extensions right.

_Checked on changed lines only (`--staged`)._

---

### verify-rule-changed

Rule/glob matching changed in <file>. _glob_match and Rule.check() affect every rule. Run: pytest tests/test_rule.py tests/test_runner.py

**Why:** rule.py contains _glob_match and Rule.check() — every rule flows through it. Changes here affect glob matching and metadata stamping for all rules.

**Applies to:** enforcer/rule.py

**Fix:** Verify: glob matching works for ** patterns, Rule.check() stamps metadata correctly.

_Checked on changed lines only (`--staged`)._

---

### verify-runner-changed

Runner changed in <file>. Cross-file finalizers and severity filtering affect all rules. Run: pytest tests/test_runner.py tests/test_metadata_rules.py

**Why:** runner.py drives severity filtering, LLM consequence execution, and cross-file finalizers. Changes here can silently change which rules fire.

**Applies to:** enforcer/runner.py

**Fix:** Verify: run_cross_file_finalizers works, severity filtering correct, LLM consequences fire.

_Checked on changed lines only (`--staged`)._

---

### verify-types-changed

Core types changed in <file>. Every matcher/predicate/combinator depends on these. Run full test suite: pytest --tb=short -q

**Why:** types.py is load-bearing — every matcher, predicate, and combinator depends on it. Changes here can break the entire rule engine silently.

**Applies to:** enforcer/types.py

**Fix:** Verify: pytest passes, no matcher breaks on new types.py.

_Checked on changed lines only (`--staged`)._

---
