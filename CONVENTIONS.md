# Conventions

_55 rules configured. Auto-generated by `enforcer sync-doc`. Do not edit by hand._

## ERROR — must fix before commit

### all-sorted

__all__ at <file>:<line> is not alphabetically sorted.

**Why:** Unsorted __all__ lists cause diff noise and make it hard to find exports. Alphabetical order is deterministic and scannable.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `AllSortedMatcher` — flags __all__ lists where entries are not in alphabetical order
  - Tests: `tests/test_matchers/test_all_sorted.py`

**Fix:** Sort the __all__ entries alphabetically.

_Checked on changed lines only (`--staged`)._

---

### arch-layer-deps

Layer violation: <matched_value> at <file>:<line>

**Why:** Importing upward creates circular deps and prevents isolated testing. Layers: types < rule/parsers/matchers/predicates/combinators/extractors < core < io.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `ArchitectureMatcher` — flags import statements whose (source_layer -> target_layer) is
  - Tests: `tests/test_matchers/test_architecture.py`

**Excludes:** enforcer/__init__.py

**Fix:** Move shared logic down to a lower layer, or add the edge to allowed_edges if intentional.

---

### branch-naming

Branch '<matched_value>' doesn't match required pattern: type/description

**Why:** Branches encode intent; CI/greps depend on the type/ prefix to route checks and changelogs.

**Applies to:** * (metadata rule, checked once per run)

**Matchers:**

- `BranchNameMatcher` — flags the current git branch when it doesn't match `pattern`
  - Tests: `tests/test_matchers/test_branch_name.py`

**Fix:** Rename: git branch -m <type>/<description>

---

### canonical-import-source

Import <matched_value> from canonical module, not from a re-exporting module.

**Why:** Re-exports create hidden coupling. Importing from the canonical low-level module keeps dependencies explicit and prevents arch-layer-deps violations.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `CanonicalImportMatcher` — flags `from <module> import <symbol>` when <symbol> is in the canonical map
  - Tests: `tests/test_matchers/test_canonical_import.py`

**Fix:** Import from the canonical source module listed in the canonical map.

---

### class-capwords

Class '<matched_value>' at <file>:<line> must be CapWords (PascalCase)

**Why:** CapWords (PascalCase) is the Python convention for classes (PEP 8). Distinguishes types from functions at a glance.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `NamingConventionMatcher` — flags declaration names (functions/classes/variables per declaration_types) that don't match `pattern`
  - Tests: `tests/test_matchers/test_naming_convention.py`

**Fix:** Rename to CapWords.

_Checked on changed lines only (`--staged`)._

---

### class-needs-interface

Class '<matched_value>' at <file>:<line> has >=4 public methods but no base class. Inherit from Protocol/ABC or a base class.

**Why:** Classes with many methods and no interface are hard to mock, test in isolation, and substitute. An interface enables polymorphism and dependency injection.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `InterfaceMatcher` — flags non-dataclass classes with >=min_methods public methods and no base class (no inheritance)

**Excludes:** enforcer/cli.py

**Fix:** Add a base class (Protocol, ABC, or domain base) to the class definition.

_Checked on changed lines only (`--staged`)._

---

### combinator-docstring-structured

Combinator class at <file>:<line> docstring missing 'What:' section.

**Why:** Combinators compose matcher logic; structured docstrings explain the composition.

**Applies to:** enforcer/combinators/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Excludes:** enforcer/combinators/__init__.py

**Fix:** Add 'What: <what it composes>' to the class docstring.

_Checked on changed lines only (`--staged`)._

---

### combinator-test-paired

No test file for combinator <file>. Every combinator needs paired tests.

**Why:** Combinators compose matcher logic; untested combinators can invert or short-circuit the intended logic.

**Applies to:** enforcer/combinators/*.py

**Matchers:**

- `PairedFileMatcher` — flags source files (matching source_glob, and — if set — with a git status in `statuses`) whose derived paired file does NOT exist on disk
  - Tests: `tests/test_matchers/test_paired_file.py`

**Excludes:** enforcer/combinators/__init__.py

**Fix:** Create tests/test_combinators/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### combinator-test-positive-negative

Test file <file> missing positive or negative parameterized coverage (>=3 cases each).

**Why:** Combinators compose matcher logic; untested combinators can invert or short-circuit the intended logic.

**Applies to:** tests/test_combinators/*.py

**Matchers:**

- `TestCoverageMatcher` — flags test files missing positive (assert) or negative (assert not) cases, or with <3 parametrize cases
  - Tests: `tests/test_matchers/test_test_coverage.py`

**Excludes:** tests/test_combinators/__init__.py

**Fix:** Add a positive case (test_*_matches, assert on match list) and negative case (test_*_no_match, assert not), each @pytest.mark.parametrize with >=3 examples.

_Checked on changed lines only (`--staged`)._

---

### commit-message

Commit message '<matched_value>' doesn't follow Conventional Commits

**Why:** Conventional Commits enable automated changelog generation and semantic versioning. Unstructured messages break tooling.

**Applies to:** * (metadata rule, checked once per run)

**Matchers:**

- `CommitMessageMatcher` — flags the commit message when its first line doesn't match `pattern`
  - Tests: `tests/test_matchers/test_commit_message.py`

**Fix:** Use: type(scope): description (e.g. feat(matchers): add X)

---

### constants-upper-case

Module-level constant '<matched_value>' at <file>:<line> must be UPPER_CASE.

**Why:** UPPER_CASE constants are the Python convention (PEP 8). They distinguish compile-time-fixed values from mutable variables at a glance.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `ConstantNamingMatcher` — flags module-level variables (not _-prefixed) that are not UPPER_CASE
  - Tests: `tests/test_matchers/test_constant_naming.py`

**Excludes:** enforcer/cli.py, enforcer/mcp_server.py

**Fix:** Rename to UPPER_CASE or prefix with _ if private.

_Checked on changed lines only (`--staged`)._

---

### conventions-md-stale

CONVENTIONS.md is stale or missing. Regenerate after changing rules.

**Why:** A stale conventions doc misleads agents — they follow rules that no longer match the actual config. The doc must be regenerated whenever RULES changes, and direct edits to CONVENTIONS.md must not drift it from the config.

**Applies to:** * (metadata rule, checked once per run)

**Matchers:**

- `DocSyncMatcher` — flags when the on-disk doc at `doc_path` differs from `shared_ctx["__rendered_doc__"]`
  - Tests: `tests/test_matchers/test_doc_sync.py`

**Fix:** Run: enforcer sync-doc

---

### core-test-paired

No test file for core module <file>.

**Why:** Core modules (rule, runner, context, config) are load-bearing; untested core changes can break every rule.

**Applies to:** enforcer/*.py

**Matchers:**

- `PairedFileMatcher` — flags source files (matching source_glob, and — if set — with a git status in `statuses`) whose derived paired file does NOT exist on disk
  - Tests: `tests/test_matchers/test_paired_file.py`

**Excludes:** enforcer/__init__.py, enforcer/matchers/**, enforcer/predicates/**, enforcer/combinators/**, enforcer/parsers/**, enforcer/extractors/**

**Fix:** Create tests/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### cyclomatic-complexity

Function at <file>:<line> has cyclomatic complexity <matched_value> (max 10). Reduce branching.

**Why:** High cyclomatic complexity means too many branches — hard to reason about, test, and maintain. Extract branches into helpers or use early returns.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `FunctionComplexityMatcher` — flags functions whose `metric` (lines/params/nesting/cyclomatic) exceeds `max_value`
  - Tests: `tests/test_matchers/test_function_complexity.py`

**Excludes:** enforcer/cli.py

**Fix:** Extract branches into helper functions or use early returns.

_Checked on changed lines only (`--staged`)._

---

### docstring-public

Function '<matched_value>' at <file>:<line> missing docstring. Public functions must be documented.

**Why:** Public functions are the API surface. Without docstrings, users (and agents) must read the implementation to understand intent — that's a failure of the contract.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `DocstringMatcher` — flags public functions (Python/TS: name not _-prefixed; Go: exported) lacking a docstring/doc comment
  - Tests: `tests/test_matchers/test_docstring.py`

**Fix:** Add a docstring: """<one-line description>."""

_Checked on changed lines only (`--staged`)._

---

### extractor-docstring-structured

Extractor class at <file>:<line> docstring missing 'What:' section.

**Why:** Extractors are pure transforms; structured docstrings explain what they extract.

**Applies to:** enforcer/extractors/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Excludes:** enforcer/extractors/__init__.py

**Fix:** Add 'What: <what it extracts>' to the class docstring.

_Checked on changed lines only (`--staged`)._

---

### extractor-test-paired

Extractor <file> has no paired test. Create tests/test_extractors/test_<stem>*.py

**Why:** Extractors are pure string transforms — trivial to test. Missing tests mean regressions in key extraction go unnoticed.

**Applies to:** enforcer/extractors/*.py

**Matchers:**

- `PairedFileMatcher` — flags source files (matching source_glob, and — if set — with a git status in `statuses`) whose derived paired file does NOT exist on disk
  - Tests: `tests/test_matchers/test_paired_file.py`

**Excludes:** enforcer/extractors/__init__.py

**Fix:** Add a test file covering happy path, empty/malformed input, and format-specific edge cases.

_Checked on changed lines only (`--staged`)._

---

### extractor-test-positive-negative

Test file <file> missing positive or negative parameterized coverage (>=3 cases each).

**Why:** Extractors are pure string transforms; untested extractors silently break key extraction.

**Applies to:** tests/test_extractors/*.py

**Matchers:**

- `TestCoverageMatcher` — flags test files missing positive (assert) or negative (assert not) cases, or with <3 parametrize cases
  - Tests: `tests/test_matchers/test_test_coverage.py`

**Excludes:** tests/test_extractors/__init__.py

**Fix:** Add a positive case (test_*_extracts, assert key in set) and negative case (test_*_absent, assert key not in set), each @pytest.mark.parametrize with >=3 examples.

_Checked on changed lines only (`--staged`)._

---

### file-max-lines

File <file> has <matched_value> lines (max 400). Split into modules.

**Why:** Files over 400 lines do too much and are hard to navigate. Splitting into focused modules improves readability and testability.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `LineCountMatcher` — flags any file whose line count exceeds `max_lines`
  - Tests: `tests/test_matchers/test_line_count_matcher.py`

**Excludes:** enforcer/cli.py

**Fix:** Extract cohesive functionality into a new module or sub-package.

_Checked on changed lines only (`--staged`)._

---

### function-max-lines

Function at <file>:<line> has <matched_value> lines (max 75). Split or extract.

**Why:** Long functions do too much and are hard to test, read, and review. Splitting forces single-responsibility and improves testability.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `FunctionComplexityMatcher` — flags functions whose `metric` (lines/params/nesting/cyclomatic) exceeds `max_value`
  - Tests: `tests/test_matchers/test_function_complexity.py`

**Excludes:** enforcer/cli.py

**Fix:** Extract sub-functions or move logic to a helper module.

_Checked on changed lines only (`--staged`)._

---

### function-max-params

Function at <file>:<line> has <matched_value> parameters (max 5). Group into a dataclass.

**Why:** More than 5 params signals the function does too much; group into a dataclass to make the boundary explicit and the call site readable.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `FunctionComplexityMatcher` — flags functions whose `metric` (lines/params/nesting/cyclomatic) exceeds `max_value`
  - Tests: `tests/test_matchers/test_function_complexity.py`

**Excludes:** enforcer/cli.py

**Fix:** Group related parameters into a dataclass and pass as single arg.

_Checked on changed lines only (`--staged`)._

---

### function-snake-case

Function '<matched_value>' at <file>:<line> must be snake_case

**Why:** snake_case is the Python convention (PEP 8). Deviating creates inconsistency that makes code harder to scan.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `NamingConventionMatcher` — flags declaration names (functions/classes/variables per declaration_types) that don't match `pattern`
  - Tests: `tests/test_matchers/test_naming_convention.py`

**Fix:** Rename to snake_case.

_Checked on changed lines only (`--staged`)._

---

### matcher-docstring-structured

Matcher class at <file>:<line> docstring missing 'What:' or 'Basis:' section.

**Why:** Matchers without structured docstrings can't be explained by `enforcer explain`. The What:/Basis: sections are the minimum for self-documentation.

**Applies to:** enforcer/matchers/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Excludes:** enforcer/matchers/__init__.py, enforcer/matchers/test_coverage.py

**Fix:** Add 'What: <what it flags>' and 'Basis: <RAW|AST_PY|AST_TS|AST_CSS>' lines to the class docstring.

_Checked on changed lines only (`--staged`)._

---

### matcher-test-paired

No test file for matcher <file>. Every matcher needs paired tests.

**Why:** Untested matchers ship false positives/negatives silently. Paired tests catch regressions before they reach users.

**Applies to:** enforcer/matchers/*.py

**Matchers:**

- `PairedFileMatcher` — flags source files (matching source_glob, and — if set — with a git status in `statuses`) whose derived paired file does NOT exist on disk
  - Tests: `tests/test_matchers/test_paired_file.py`

**Excludes:** enforcer/matchers/__init__.py

**Fix:** Create tests/test_matchers/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### matcher-test-positive-negative

Test file <file> missing positive or negative parameterized coverage (>=3 cases each).

**Why:** Matchers enforce conventions; tests enforce matchers. Without both positive and negative parameterized cases, matcher regressions go undetected.

**Applies to:** tests/test_matchers/*.py

**Matchers:**

- `TestCoverageMatcher` — flags test files missing positive (assert) or negative (assert not) cases, or with <3 parametrize cases
  - Tests: `tests/test_matchers/test_test_coverage.py`

**Excludes:** tests/test_matchers/__init__.py, tests/test_matchers/test_test_coverage.py

**Fix:** Add a positive case (test_*_fail, assert on match list) and negative case (test_*_success, assert not), each @pytest.mark.parametrize with >=3 examples.

_Checked on changed lines only (`--staged`)._

---

### matchers-no-import-runner-cli

Import layer violation at <file>:<line>: matchers must not import from runner/cli/mcp_server/reporter/fix/docs/explain/config.

**Why:** Matchers are low-level building blocks. If they import from higher layers (runner, cli), they become impossible to reuse in isolation or test without pulling the entire stack.

**Applies to:** enforcer/matchers/*.py

**Matchers:**

- `ImportMatcher` — flags import statements whose text matches any of `forbidden_patterns`
  - Tests: `tests/test_matchers/test_import_matcher.py`

**Excludes:** enforcer/matchers/__init__.py

**Fix:** Move the shared logic down to types.py, a new low-level module, or pass the dependency as a parameter.

_Checked on changed lines only (`--staged`)._

---

### max-nesting-depth

Function at <file>:<line> has nesting depth <matched_value> (max 3). Flatten with early returns or extract helpers.

**Why:** Deep nesting is hard to read, test, and maintain. Guard clauses and extraction keep functions flat and scannable.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `FunctionComplexityMatcher` — flags functions whose `metric` (lines/params/nesting/cyclomatic) exceeds `max_value`
  - Tests: `tests/test_matchers/test_function_complexity.py`

**Excludes:** enforcer/cli.py

**Fix:** Extract nested logic into helper functions or use early returns/guard clauses.

_Checked on changed lines only (`--staged`)._

---

### no-bare-except

Bare except: at <file>:<line>. Use except Exception or more specific.

**Why:** Bare except catches SystemExit and KeyboardInterrupt, masking intentional exits and making debugging impossible.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Fix:** Change to `except Exception:` or a more specific exception.

---

### no-bare-type-ignore

Bare `# type: ignore` at <file>:<line> silences errors without explanation.

**Why:** Bare `# type: ignore` hides real type errors and spreads — future readers can't tell if the ignore is still needed. A reason forces the author to justify the suppression.

**Applies to:** **/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Fix:** Add a reason: `# type: ignore[<error-code>]  # <why>`

_Checked on changed lines only (`--staged`)._

---

### no-debug-code

Debug code at <file>:<line>. Remove before commit.

**Why:** Debug code left in production halts execution and blocks CI. A single forgotten breakpoint can page someone at 3am.

**Applies to:** **/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Excludes:** **/test*, **/*test*

**Fix:** Remove breakpoint()/pdb.set_trace() or wrap in `if DEBUG:` guard.

---

### no-duplicate-rule-ids

Duplicate Rule id '<matched_value>' in config. Each id must be unique.

**Why:** Duplicate rule IDs silently shadow each other — one rule's config overwrites the other's in any id-keyed lookup.

**Applies to:** enforcer_config/__init__.py

**Matchers:**

- `DuplicateRuleIdMatcher` — flags Rule id= values that appear 2+ times in the same file
  - Tests: `tests/test_matchers/test_duplicate_rule_id.py`

**Fix:** Rename one of the duplicate rules to a unique id.

---

### no-magic-numbers

Magic number <matched_value> at <file>:<line>. Extract to a named constant.

**Why:** Magic numbers are unexplained literals. Without a name, their meaning is opaque. A named constant documents intent and centralizes change.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `MagicNumberMatcher` — flags numeric literals (integers outside -5..5, all floats) in function bodies, not in assignments or parameter defaults
  - Tests: `tests/test_matchers/test_magic_number.py`

**Excludes:** enforcer/cli.py, enforcer/mcp_server.py, enforcer/types.py

**Fix:** Assign to an UPPER_CASE constant: `MAX_VALUE = 42` then use `MAX_VALUE`.

_Checked on changed lines only (`--staged`)._

---

### no-module-side-effects

Module-level side effect at <file>:<line>: '<matched_value>' statement runs at import time.

**Why:** Module-level side effects (calls, loops, prints) run at import time, breaking isolation, making tests unpredictable, and causing import-order bugs.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `NoModuleSideEffectsMatcher` — flags module-level statements that aren't imports, assignments, class/function defs, or conditionals
  - Tests: `tests/test_matchers/test_no_module_side_effects.py`

**Excludes:** enforcer/cli.py, enforcer/mcp_server.py

**Fix:** Move the side effect into a function or class method. Import-time execution breaks isolation and makes testing unpredictable.

_Checked on changed lines only (`--staged`)._

---

### no-print

print() found in library code at <file>:<line>. Use sys.stderr.write or structlog.

**Why:** print() writes to stdout, which is reserved for machine-readable output in CLI tools. Mixing human prose into stdout breaks piping and scripting. sys.stderr is the correct channel for human-facing diagnostics.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Fix:** Replace print() with sys.stderr.write(...).

---

### no-private-cross-import

Private import at <file>:<line>: importing _-prefixed names from other modules breaks encapsulation.

**Why:** Importing private symbols creates hidden coupling. If a module needs a _-prefixed name, it either belongs in a shared module or should be made public.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `ImportMatcher` — flags import statements whose text matches any of `forbidden_patterns`
  - Tests: `tests/test_matchers/test_import_matcher.py`

**Fix:** Make the imported symbol public (remove _ prefix) or move the shared logic to a common module.

_Checked on changed lines only (`--staged`)._

---

### no-scratch-files

Scratch/debug file at repo root: <matched_value>. Delete it.

**Why:** Committed scratch files trigger style violations and clutter the repo root.

**Applies to:** scratch_*.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Delete the file. Use /tmp or a git-ignored directory for scratch work.

---

### no-secrets

Possible hardcoded secret at <file>:<line>. Use env var.

**Why:** Hardcoded secrets ship to the repo and can't be rotated without a commit. Env vars separate config from code and keep secrets out of version control.

**Applies to:** **/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Excludes:** **/test*, **/*test*

**Fix:** Move to env var or secrets manager.

---

### no-wildcard-imports

Wildcard import at <file>:<line>. Use explicit imports.

**Why:** Wildcard imports pollute the namespace and hide dependencies. Explicit imports make it clear where symbols come from and avoid name collisions.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `ImportMatcher` — flags import statements whose text matches any of `forbidden_patterns`
  - Tests: `tests/test_matchers/test_import_matcher.py`

**Fix:** Replace `from X import *` with explicit symbol imports.

_Checked on changed lines only (`--staged`)._

---

### predicate-docstring-structured

Predicate class at <file>:<line> docstring missing 'What:' or 'Basis:' section.

**Why:** Predicates filter matches; structured docstrings explain what they pass and why.

**Applies to:** enforcer/predicates/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Excludes:** enforcer/predicates/__init__.py

**Fix:** Add 'What: <what it passes>' and 'Basis: <RAW|AST_PY|AST_TS|AST_CSS>' lines to the class docstring.

_Checked on changed lines only (`--staged`)._

---

### predicate-test-paired

No test file for predicate <file>. Every predicate needs paired tests.

**Why:** Predicates filter matches; untested predicates can silently suppress real violations or let false ones through.

**Applies to:** enforcer/predicates/*.py

**Matchers:**

- `PairedFileMatcher` — flags source files (matching source_glob, and — if set — with a git status in `statuses`) whose derived paired file does NOT exist on disk
  - Tests: `tests/test_matchers/test_paired_file.py`

**Excludes:** enforcer/predicates/__init__.py

**Fix:** Create tests/test_predicates/test_{stem}.py

_Checked on changed lines only (`--staged`)._

---

### predicate-test-positive-negative

Test file <file> missing positive or negative parameterized coverage (>=3 cases each).

**Why:** Predicates filter matches; untested predicates can silently suppress real violations or let false ones through.

**Applies to:** tests/test_predicates/*.py

**Matchers:**

- `TestCoverageMatcher` — flags test files missing positive (assert) or negative (assert not) cases, or with <3 parametrize cases
  - Tests: `tests/test_matchers/test_test_coverage.py`

**Excludes:** tests/test_predicates/__init__.py

**Fix:** Add a positive case (test_*_passes, assert True) and negative case (test_*_fails, assert not), each @pytest.mark.parametrize with >=3 examples.

_Checked on changed lines only (`--staged`)._

---

### public-function-return-type

Function '<matched_value>' at <file>:<line> missing return type annotation.

**Why:** Return type annotations document the contract and enable static analysis. Without them, callers must read the implementation to know what a function returns.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `TypeHintMatcher` — flags public functions (not _-prefixed) whose definition lacks a -> return type annotation
  - Tests: `tests/test_matchers/test_type_hint.py`

**Excludes:** enforcer/cli.py, enforcer/mcp_server.py

**Fix:** Add `-> ReturnType` to the function signature.

_Checked on changed lines only (`--staged`)._

---

### readme-max-lines

README.md has <matched_value> lines (max 300). LLM analyzed what doesn't belong.

**Why:** A README over 300 lines is too long for a landing doc. Bloat hides the getting-started path; details belong in docs/.

**Applies to:** README.md

**Matchers:**

- `LineCountMatcher` — flags any file whose line count exceeds `max_lines`
  - Tests: `tests/test_matchers/test_line_count_matcher.py`

**Fix:** Remove or trim the sections flagged by the LLM response below.

**LLM check:** You are reviewing a README.md that exceeds 300 lines. Identify the specific sections that don't belong in a README and make it too long. For each section, explain why it should be removed or trimmed. Be concrete — reference section headings and line ranges. Common bloat: full install logs, API reference dumps, changelogs, verbose examples, duplicated content.
**Model:** None

---

### rule-no-import-up

Import layer violation at <file>:<line>: rule.py must not import from runner/cli/mcp_server/reporter/fix/docs/explain/config.

**Why:** rule.py defines Rule — the core unit of composition. Importing from runner or cli creates a circular dependency and prevents isolated testing.

**Applies to:** enforcer/rule.py

**Matchers:**

- `ImportMatcher` — flags import statements whose text matches any of `forbidden_patterns`
  - Tests: `tests/test_matchers/test_import_matcher.py`

**Fix:** Move the shared logic down to types.py or a new low-level module.

_Checked on changed lines only (`--staged`)._

---

### runner-no-import-cli

Import layer violation at <file>:<line>: runner.py must not import from cli or mcp_server.

**Why:** runner.py applies rules to files. Importing from cli or mcp_server (entrypoints) creates a circular dependency and prevents reuse.

**Applies to:** enforcer/runner.py

**Matchers:**

- `ImportMatcher` — flags import statements whose text matches any of `forbidden_patterns`
  - Tests: `tests/test_matchers/test_import_matcher.py`

**Fix:** Move the shared logic to a mid-level module that both runner and cli can import.

_Checked on changed lines only (`--staged`)._

---

### todo-needs-owner

TODO/FIXME without owner at <file>:<line>. Use '# TODO(@name): …' or remove.

**Why:** TODOs without owners never get done. An owner reference makes responsibility explicit and enables grepping for open work.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `RegexMatcher` — flags any line where `pattern` matches at least once
  - Tests: `tests/test_matchers/test_regex_matcher.py`

**Fix:** Add owner reference or delete the TODO and address now.

_Checked on changed lines only (`--staged`)._

---

## WARN — blocks unless acknowledged

### commit-msg-aligns-with-changes

Commit message may not align with changes. LLM: <matched_value>

**Why:** A commit message that doesn't describe the actual changes misleads future archaeologists using git log/blame. The LLM sanity check catches gross mismatches.

**Applies to:** * (metadata rule, checked once per run)

**Matchers:**

- `LLMMatcher` — flags violations reported by the LLM (parsed from JSON `violations` array, or FAIL text fallback)

**Fix:** Rewrite commit message to describe the actual changes.

---

### facade-exists

Submodule <file> has no facade (__init__.py)

**Why:** Every submodule should have a facade (__init__.py) that re-exports its public API. This enables clean imports and hides internal structure.

**Applies to:** enforcer/**/*.py

**Matchers:**

- `FacadeExistsMatcher` — flags files matching source_glob whose parent dir lacks {facade}
  - Tests: `tests/test_matchers/test_facade_exists.py`

**Excludes:** enforcer/__init__.py

**Fix:** Create enforcer/{file}/__init__.py re-exporting the public API.

---

### facade-exposes-interface

Facade <file> exposes no interface (Protocol/ABC or __all__)

**Why:** Facades should expose a public interface (Protocol/ABC) or at minimum an __all__ re-export. This documents the contract and enables dependency injection.

**Applies to:** enforcer/*/__init__.py

**Matchers:**

- `FacadeExposesInterfaceMatcher` — flags files with no class(Protocol) or class(ABC) declaration,
  - Tests: `tests/test_matchers/test_facade_exposes_interface.py`

**Fix:** Add a Protocol/ABC class or __all__ re-export to the facade.

---

### verify-config-changed

Config loader changed in <file>. load_config executes enforcer_config as module. Run: pytest tests/test_config.py

**Why:** config.py executes enforcer_config as a module. Changes here affect how every rule is loaded.

**Applies to:** enforcer/config.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Verify: RULES/WORKSPACE/SEVERITY_ACTIONS/LLM_CONFIG extracted correctly, defaults work.

_Checked on changed lines only (`--staged`)._

---

### verify-context-changed

FileContextBuilder changed in <file>. Parse-once cache drives all AST matchers. Run: pytest tests/test_context.py tests/test_parse_once.py

**Why:** context.py owns the parse-once cache. A broken cache means every AST matcher re-parses or gets stale ASTs.

**Applies to:** enforcer/context.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Verify: AST populated lazily, cache hits don't reparse, needs_for_file aggregates correctly.

_Checked on changed lines only (`--staged`)._

---

### verify-parser-changed

Parser changed in <file>. Tree-sitter parse affects all AST matchers. Run: pytest tests/test_parsers.py tests/test_parse_once.py

**Why:** The tree-sitter parser feeds all AST matchers. Changes here can silently break AST detection for Python, TS, or CSS.

**Applies to:** enforcer/parsers/*.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Verify: Python/TS/CSS ASTs parse correctly, language_for_path maps extensions right.

_Checked on changed lines only (`--staged`)._

---

### verify-rule-changed

Rule/glob matching changed in <file>. _glob_match and Rule.check() affect every rule. Run: pytest tests/test_rule.py tests/test_runner.py

**Why:** rule.py contains _glob_match and Rule.check() — every rule flows through it. Changes here affect glob matching and metadata stamping for all rules.

**Applies to:** enforcer/rule.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Verify: glob matching works for ** patterns, Rule.check() stamps metadata correctly.

_Checked on changed lines only (`--staged`)._

---

### verify-runner-changed

Runner changed in <file>. Cross-file finalizers and severity filtering affect all rules. Run: pytest tests/test_runner.py tests/test_metadata_rules.py

**Why:** runner.py drives severity filtering, LLM consequence execution, and cross-file finalizers. Changes here can silently change which rules fire.

**Applies to:** enforcer/runner.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Verify: run_cross_file_finalizers works, severity filtering correct, LLM consequences fire.

_Checked on changed lines only (`--staged`)._

---

### verify-types-changed

Core types changed in <file>. Every matcher/predicate/combinator depends on these. Run full test suite: pytest --tb=short -q

**Why:** types.py is load-bearing — every matcher, predicate, and combinator depends on it. Changes here can break the entire rule engine silently.

**Applies to:** enforcer/types.py

**Matchers:**

- `AlwaysMatcher` — flags every file it runs on (always emits one Match with matched_value)
  - Tests: `tests/test_matchers/test_always_matcher.py`

**Fix:** Verify: pytest passes, no matcher breaks on new types.py.

_Checked on changed lines only (`--staged`)._

---
